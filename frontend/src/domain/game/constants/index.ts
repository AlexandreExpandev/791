import { Choice } from '../types';

/**
 * @constant CHOICES
 * @summary An array of available choices in the game.
 * @domain game
 * @type constant
 */
export const CHOICES: Choice[] = ['pedra', 'papel', 'tesoura'];
